<%= render 'comment', comment: @comment %>
<%= render 'form', comment: @new_comment %>

<% @comments.each do |comment| %>
  	<%= render 'comment', comment: comment %>
<% end %>

<%= will_paginate @comments, previous_label: "Anterior", next_label: "Siguiente", class: "apple_pagination" %>

